# Hamlog ko print() function ke under agar value ko print karwana hai to double quotes ke under nahi rakhenge.
# Double quotes ke under rakhne se jo braket ke under hai wahi print ho jayega.
a=5
b=10
c=a+b
print("Addition =",c)

# Variable is using to store the values, in python we do not need to mention datatype with values like int,
# float, str
# Variable name should be start from letter and underscore
# Variable name should not be start from number
# variable name contain only alphanumeric character and underscore only(A-Z, 0-9, underscore).
# No space allowed between variable name
# Every looping statement has 3 parts like initialization, condition and increment/decrement.
# if any parts is missing in the loop then loop will not execute
# Strings are immutable, u can not change an individual letters of string using assignment operator.
# end="" if u want to print in one line then we need to put end="" under in print() function
# if u want to print characterwise then use for loop
# We can concatinate two strings like string1+string2 but not add string+integer
# Relication operator (*):- 3*"hello", output=hellohellohello
# Membership operator(in, not in)
  #str1="Ram"
 # a in "str1"
# list ke max() and min() function ke liye same datatype ka item rahega tabhi max or min kaam karega otherwise error dega.
#

